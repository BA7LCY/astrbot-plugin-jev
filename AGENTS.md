## QQ聊天bot导演系统

### 项目介绍
待定

### 项目规范
- 代码尽量使用依赖注入模式，以便进行充分的mock测试
- 尽量使用成熟的开源框架加速开发 而不是从零构建
- 代码架构需要保证充分的可维护性 尤其是考虑到llm上下文长度受限的可维护性
- 修改本项目以外的事物需要审批，如系统设置、项目外文件等
- 不直接对接Astrbot插件系统，制作兼容层以便迁移到其他框架
- 优先使用git bash作为命令行工具
- 使用中文进行注释、文档、对话
- 每次改动完成需要提交到git仓库，重置、回退git版本等危险操作必须让用户审核
- 修改WebUI时，请确保保持组件化和干净的代码。避免重复代码
- 考虑跨平台兼容性（例如，Windows、macOS和Linux，以及Arm 64和x86中央处理器架构），并与Python 3.10+兼容。
- 对于路径处理，使用`pathlib.Path`代替字符串路径，并使用`astrobot.core.utils.path_utils`获取AstrBot数据和临时目录。

### 部署流程
使用scripts/deploy_git.sh脚本部署项目